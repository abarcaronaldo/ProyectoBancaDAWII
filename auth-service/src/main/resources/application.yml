spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/proyectobanca?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

app:
  jwt:
    secret: "Cibertec_ProyectoIntegrador_Microservicios_ExamenFinal_2026_KeySecure!"
    expiration-minutes: 60
